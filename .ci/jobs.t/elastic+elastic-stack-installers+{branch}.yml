---

- job:
    name: elastic+elastic-stack-installers+%BRANCH%
    display-name: 'elastic / elastic-stack-installers # %BRANCH%'
    description: Testing the elastic-stack-installers %BRANCH% branch.
    triggers:
      - github
      - timed: "@daily"
    builders:
      - powershell: |
          echo "-- COMMIT 8< ----------------------------"
          ./build show
          echo "-- COMMIT 8< ----------------------------"
    publishers:
      - email:
          recipients: infra-root+build@elastic.co
      - slack:
          notify-back-to-normal: True
          notify-every-failure: True
          room: github-stack-installers
          team-domain: elastic
          auth-token-id: release-slack-integration-token
          auth-token-credential-id: release-slack-integration-token
